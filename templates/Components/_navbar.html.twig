<nav>
	{% for event in events %}
		<a href="/announcements/region/{{region.id}}/{% if selected %}region_id={{selected}}{% endif %}{% if event!='tous' %}{{event}}{% endif %}" {% if active==event %} class="active" {% endif %}>{{event}}</a>
	{% endfor %}
</nav>
